---
const steps = [
  {
    number: "01",
    title: "Regístrate Instantáneamente",
    description: "Crea tu cuenta en menos de 2 minutos. Obtén tu código de referencia único y configura tus cuentas bancarias.",
    icon: "👤",
    color: "primary"
  },
  {
    number: "02",
    title: "Deposita con tu Código",
    description: "Realiza tu depósito incluyendo tu código único en el concepto. Nuestro sistema te identifica automáticamente.",
    icon: "💸",
    color: "pink"
  },
  {
    number: "03",
    title: "Procesamiento Inteligente",
    description: "Nuestros operadores registran tu transacción y el sistema calcula las comisiones al instante.",
    icon: "⚙️",
    color: "cyan"
  },
  {
    number: "04",
    title: "Recibe tu Pago",
    description: "Transferencia directa a tu cuenta con notificación instantánea. Transparencia total en cada paso.",
    icon: "✓",
    color: "green"
  }
];
---

<section class="process" id="proceso">
  <div class="process-container">
    <div class="process-header">
      <span class="section-tag">PROCESO</span>
      <h2 class="section-title">
        Simple, <span class="highlight">Rápido</span> y Seguro
      </h2>
      <p class="section-description">
        Diseñado para que hagas más en menos tiempo. 4 pasos y listo.
      </p>
    </div>

    <div class="timeline">
      <div class="timeline-line"></div>
      
      {steps.map((step, index) => (
        <div class="step-container" style={`animation-delay: ${index * 0.2}s`}>
          <div class="step-card" data-color={step.color}>
            <div class="step-number">{step.number}</div>
            <div class="step-icon">{step.icon}</div>
            <h3 class="step-title">{step.title}</h3>
            <p class="step-description">{step.description}</p>
            <div class="step-pulse"></div>
          </div>
          {index < steps.length - 1 && (
            <div class="step-connector"></div>
          )}
        </div>
      ))}
    </div>

    <div class="process-footer">
      <div class="footer-card">
        <div class="footer-icon">⏱️</div>
        <div class="footer-content">
          <h4>Procesamiento Ultra-Rápido</h4>
          <p>La mayoría de transacciones se completan el mismo día</p>
        </div>
      </div>
      <div class="footer-card">
        <div class="footer-icon">🔒</div>
        <div class="footer-content">
          <h4>Seguridad Garantizada</h4>
          <p>Tus datos protegidos con encriptación de nivel bancario</p>
        </div>
      </div>
      <div class="footer-card">
        <div class="footer-icon">📱</div>
        <div class="footer-content">
          <h4>Siempre Conectado</h4>
          <p>Accede desde cualquier dispositivo, en cualquier momento</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .process {
    padding: 8rem 2rem;
    background: radial-gradient(circle at 50% 0%, rgba(0, 169, 157, 0.05) 0%, transparent 50%);
    position: relative;
    overflow: hidden;
  }

  .process::before {
    content: '';
    position: absolute;
    top: -300px;
    right: -200px;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(26, 77, 109, 0.1) 0%, transparent 70%);
    filter: blur(80px);
  }

  .process-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .process-header {
    text-align: center;
    margin-bottom: 6rem;
  }

  .section-tag {
    display: inline-block;
    padding: 0.5rem 1.25rem;
    background: rgba(0, 169, 157, 0.1);
    border: 1px solid rgba(0, 169, 157, 0.3);
    border-radius: 50px;
    color: var(--primary-light);
    font-weight: 700;
    font-size: 0.875rem;
    letter-spacing: 0.1em;
    margin-bottom: 1.5rem;
  }

  .section-title {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 4rem;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .highlight {
    background: linear-gradient(135deg, var(--primary), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-description {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .timeline {
    position: relative;
    max-width: 900px;
    margin: 0 auto 6rem;
  }

  .timeline-line {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 2px;
    height: 100%;
    background: linear-gradient(180deg, 
      var(--primary) 0%, 
      var(--accent-pink) 50%, 
      var(--accent) 100%
    );
    opacity: 0.3;
  }

  .step-container {
    position: relative;
    margin-bottom: 4rem;
    opacity: 0;
    animation: fadeInScale 0.8s ease-out forwards;
  }

  @keyframes fadeInScale {
    to {
      opacity: 1;
      transform: scale(1);
    }
    from {
      opacity: 0;
      transform: scale(0.9);
    }
  }

  .step-card {
    position: relative;
    max-width: 400px;
    margin: 0 auto;
    padding: 2.5rem;
    background: rgba(20, 20, 31, 0.8);
    border: 1px solid var(--border);
    border-radius: 24px;
    backdrop-filter: blur(20px);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
  }

  .step-card:hover {
    transform: translateY(-10px) scale(1.02);
    border-color: var(--primary);
    box-shadow: 0 25px 70px rgba(99, 102, 241, 0.4);
  }

  .step-card[data-color="pink"]:hover {
    border-color: var(--accent-pink);
    box-shadow: 0 25px 70px rgba(236, 72, 153, 0.4);
  }

  .step-card[data-color="cyan"]:hover {
    border-color: var(--accent-cyan);
    box-shadow: 0 25px 70px rgba(6, 182, 212, 0.4);
  }

  .step-card[data-color="green"]:hover {
    border-color: var(--secondary);
    box-shadow: 0 25px 70px rgba(16, 185, 129, 0.4);
  }

  .step-number {
    position: absolute;
    top: -20px;
    right: 2rem;
    font-family: 'Space Grotesk', sans-serif;
    font-size: 4rem;
    font-weight: 800;
    background: linear-gradient(135deg, var(--primary), var(--accent-pink));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0.2;
  }

  .step-icon {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, rgba(0, 169, 157, 0.2), rgba(26, 77, 109, 0.2));
    border: 1px solid var(--border);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .step-card:hover .step-icon {
    transform: scale(1.15) rotate(-5deg);
  }

  .step-title {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .step-description {
    color: var(--text-secondary);
    line-height: 1.8;
    font-size: 1.0625rem;
  }

  .step-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    border: 2px solid var(--primary);
    border-radius: 24px;
    opacity: 0;
    animation: pulse-ring 2s ease-out infinite;
  }

  @keyframes pulse-ring {
    0% {
      opacity: 0.6;
      transform: translate(-50%, -50%) scale(0.95);
    }
    100% {
      opacity: 0;
      transform: translate(-50%, -50%) scale(1.1);
    }
  }

  .process-footer {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .footer-card {
    display: flex;
    gap: 1.5rem;
    padding: 2rem;
    background: rgba(20, 20, 31, 0.5);
    border: 1px solid var(--border);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .footer-card:hover {
    border-color: var(--primary);
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(99, 102, 241, 0.2);
  }

  .footer-icon {
    flex-shrink: 0;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, rgba(0, 169, 157, 0.2), rgba(26, 77, 109, 0.2));
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.75rem;
  }

  .footer-content h4 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.125rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .footer-content p {
    color: var(--text-secondary);
    font-size: 0.9375rem;
    line-height: 1.6;
  }

  @media (max-width: 1024px) {
    .section-title {
      font-size: 3rem;
    }

    .timeline {
      max-width: 100%;
    }

    .step-card {
      max-width: 500px;
    }
  }

  @media (max-width: 640px) {
    .process {
      padding: 5rem 1.5rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .timeline-line {
      left: 40px;
    }

    .step-container {
      margin-bottom: 3rem;
    }

    .step-card {
      max-width: 100%;
      padding: 2rem;
    }

    .step-number {
      font-size: 3rem;
      top: -15px;
      right: 1.5rem;
    }

    .step-icon {
      width: 60px;
      height: 60px;
      font-size: 2rem;
    }

    .step-title {
      font-size: 1.5rem;
    }

    .footer-card {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }
  }
</style>
